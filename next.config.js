/** @type {import('next').NextConfig} */
const nextConfig = {
  locales: ['en-US'],
  defaultLocale: 'en-US',
  reactStrictMode: true,
  swcMinify: true,
  pageExtensions: ['js']
}

module.exports = nextConfig
